# 課題

## 問題

`useParams`を使って、URLに含まれる`id`を取得して表示してください。

## 条件


* `/user/:id`のRouteを作成する
* `User`コンポーネントを作成する
* `useParams`を使用する
* URLの`id`を取得する
* `User ID: ○○`と表示する

### 例

```text
/user/123
```

↓

```text
User ID: 123
```